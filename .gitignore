# libraries are fetched, not shipped (see ATTRIBUTION.md)
/data/ldraw/
/data/shadow/
/data/*.json
/data/*.bin

# build
/bin/
# the built binary at the repo root; anchored so it does not swallow cmd/brickmesh/
/brickmesh

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
